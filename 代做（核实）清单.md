# 代做（核实）清单

> 项目 CI/CD、部署、测试、运行时风险待办。完成标 [x]。**每条带 file:line 溯源锚点**；完整核实判定 / 命令 / 日志证据见 `.claude/memory/archive-deprecated.md` 归档 Q（baime OCA，2026-07-03）。
>
> 🔴 **P0 安全已短期修复**（2026-07-03）：JWT_SECRET 改 43 字符随机值 + `frontend/nginx.conf` `access_log off`。根治（短期 ticket / header token、PG 密码 `${POSTGRES_PASSWORD:?}`）见运维安全章。

## CI/CD

- [ ] 根 CI 缺失：根 `.github/workflows/` 不存在，workflow 全在 `hermes-agent/.github/workflows/`（16 个），根项目不触发
- [ ] 补根 `.github/workflows/`（钉 `node-version: 22`，覆盖 `platform/tests` + 前端 build + compose 静态检查）
- [ ] CI Node ≥20/22（本机 18；`hermes-agent/package.json:43` `engines.node>=20`；Dockerfile `node:22`）
- [ ] CI 缺 `uv`（`pytest` 已装 9.1.1）—— 原描述"缺 uv 和 pytest"过期

## 部署脚本

- [ ] 端口统一：`deploy_docker.py:498` + `build_once.py:264` 默认 `8080→9900`（compose 实际 `9900:8080`）
- [x] docker_result NameError 死分支（`deploy_docker.py:118-123`，TASK-3 / `6c6ccefff`）
- [x] `--clean` 前缀 `openclaw-user-`→`hermes-user-`（`deploy_docker.py:382`，TASK-3 / `6c6ccefff`）
- [ ] 拆分 `docker-compose.override.yml`（⚪ L5-7 `pgdata_new` + L16-25 8180-8182 本机专配；L11 provider 修复需留）
- [ ] `build_once.py:264` 端口 + 健康检查 + README 同步 9900

## Docker 构建

- [ ] `manage_front/Dockerfile:9` `npm install`→`npm ci`
- [ ] `platform/Dockerfile:23` PIP_INDEX ARG 生效（去硬编码清华源；`:5` ARG 已在、`:17` 已用）
- [x] ghfast.top 改 `ARG GITHUB_MIRROR` 参数化（`Dockerfile:72-73,81`，TASK-3 / `6c6ccefff`）
- [ ] `Dockerfile.bridge` chown 覆盖（⚪ `:65` `web/node_modules`+`hermes_cli`、`:83-84` `.venv`/`ui-tui`/`node_modules`，已覆盖完整）
- [ ] **重建 hermes 镜像**（① bridge：edge-tts commit `3e212d9ab` 07-02；② hermes-agent 主镜像：含 #49 run.py/adapter.py 改动，2026-07-06 build 进行中）⚠️ build arg 三件套必传（`Dockerfile:31/32/66` ARG，不传走官方源 CN 慢/挂）：
  - `--build-arg GITHUB_MIRROR=https://ghfast.top/`（s6-overlay 否则 github 超时挂，#42）
  - `--build-arg APT_DEBIAN_MIRROR=https://mirrors.ustc.edu.cn/debian --build-arg APT_SECURITY_MIRROR=https://mirrors.ustc.edu.cn/debian-security`（apt 否则直连 deb.debian.org，CN 服务器 ffmpeg/gcc 244 包 ~50min）
  - `--build-arg PIP_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple`（pip uv 装依赖加速）
  - 本次 build 漏传 APT/PIP mirror，apt 阶段 244 包 ~50min，整体 build 60-80min。下次必带全套
- [x] LLM provider 根因（`config.py:57` + override env `PLATFORM_DEDICATED_HERMES_DEFAULT_PROVIDER=platform-gateway`，2026-07-02）

## 测试覆盖

- [ ] `tests/conftest.py:40` + `tests/README.md:10/30/37` 默认 `OPENCLAW_BASE_URL` `8080→9900`
- [ ] 部署脚本单测（⚪ `tests/` `platform/tests/` grep `deploy_docker|build_once` 无匹配）
- [ ] compose 一致性测试（⚪ 解析 `docker compose config` 断言端口）
- [ ] e2e 测试（⚪ 注册/登录/建容器/终端/技能/下载/飞书 onboarding）
- [ ] `platform/tests` 16 处 skip 补 DB mock（`test_hermes_runtime.py:590/637/674/714/1384/1723/1788/1841/1872/2371/2591/2780` + `test_hermes_compat.py:72`，写操作路径优先）
- [ ] 写操作测试遵守 FakeDb `commit` 约定（规则项，保持）

## 运维与安全

- [ ] SSE/WS query token 泄漏（✅短期修 `access_log off`；根治 ticket/header。源：`api.ts:447`/`Terminal.tsx:50`/`Dashboard.tsx:102`/`NotificationProvider.tsx:142`）
- [ ] 文件下载/预览 token 泄漏（✅短期修；根治 blob/ticket。源：`FileDownloadPlugin.tsx:284,300`）
- [x] JWT_SECRET / PG 密码强制覆盖（✅JWT_SECRET `compose:45` 已修；✅PG `compose:19` `${POSTGRES_PASSWORD:?}` + `.env`，2026-07-03）
- [ ] gateway CancelledError（⚪ 13 条/5000 行 logs，"connection cleanup" 0 条，SSE 断开预期噪音）

## 技能版本管理与分发（文档 8.2 一键分发地基）

> 三条代码断言全准确（行号校正见归档 Q）。改动集中 `hermes_skills.py` 单文件，冲突风险低。

- [ ] **第一步**：SKILL.md version 入解析白名单（`hermes_skills.py:177`）+ dir_fingerprint 保留输出（`:518`），~10 行
- [ ] **第二步**：git 锁 commit（`:599`）+ 补 update 路由（`openclaw_compat.py` 路由 list/delete/download/toggle/upload/search/recommended 无 update），~70 行
- [ ] zip 版本快照 + 回滚（`_put_skill_archive` `:311-337` 改非覆盖）
- [ ] 一键分发（依赖上面，缺跨用户/跨容器接口 + 前端 UI）

## 模型配置（A 全局默认 + B 单用户热切，已定方案）

> 机制：hermes `_create_agent` 每 run 读 config.yaml，改 config 下一条消息立即生效、不重启不重建（`api_server.py:1102-1103`）。env 只在创建容器时一次性写 config。

**A. 全局默认（仅新容器，零中断）**：`manager.py create_container` 改读 `get_default_model(db)`（`model_config.py:167`），~30 行。plan：`~/.claude/plans/whimsical-wishing-quilt.md`

**B. 单用户热切**：`POST /users/{id}/model` → `docker exec sed` 该用户 config.yaml，~70 行。⚠️ 临时改，destroy 重建回退全局默认。

## 当前已验证事实

- [x] compose 五服务 Up，postgres healthy
- [x] `9900/api/ping` → pong
- [x] `8080/api/ping` 失败（旧默认端口）
- [x] `py_compile` / `compileall` 通过
- [x] JWT_SECRET 改 43 字符随机值（2026-07-03）
- [x] nginx `access_log off`（2026-07-03）
