# 代做（核实）清单

> 记录项目 CI/CD、部署、测试和运行时风险的待核实事项。完成后在对应条目前标记结果，并把关键验证命令或证据补到备注里。

## CI/CD

- [ ] 核实 GitHub Actions 是否在当前仓库根目录生效：当前 workflow 位于 `hermes-agent/.github/workflows/`，如果 GitHub repo 根是本目录，则不会自动触发。
- [ ] 为 MultiUserClaw 根项目补根目录 `.github/workflows/`：至少覆盖 `platform/tests`、根 `tests`、`frontend`/`simple_front`/`manage_front` build、Docker/Compose 静态检查。
- [ ] 核实 CI 的 Node/Python 版本：前端和 `hermes-agent` 需要 Node 20/22 级别，当前本机 Node 为 18，不能代表真实构建结果。
- [ ] 核实 CI 是否能安装 `uv`、`pytest` 并运行 Python 测试；当前本机环境缺 `uv` 和 `pytest`。

## 部署脚本

- [ ] 统一 gateway 默认端口：`docker-compose.yml` 实际是 `9900:8080`，但 `deploy_docker.py` / `build_once.py` / 根 `tests` 文档仍默认 `8080`。
- [ ] 修复 `deploy_docker.py` 的 `NANOBOT_AUTO_START_DOCKER_DESKTOP=1` 分支：当前引用未定义的 `docker_result`。
- [ ] 修复 `deploy_docker.py --clean` 的用户容器清理：当前查 `openclaw-user-`，Hermes 实际前缀是 `hermes-user-`。
- [ ] 拆分本机专用 `docker-compose.override.yml`：当前 tracked override 含 `pgdata_new` 和 `8180/8181/8182` 云安全组映射，建议移到显式 compose 文件。
- [ ] 核实 `build_once.py` 的默认端口、健康检查和 README 示例是否同步到当前部署端口。

## Docker 构建

- [ ] `manage_front/Dockerfile` 改用 `npm ci`，避免锁文件与镜像构建结果不一致。
- [ ] `platform/Dockerfile` 统一 `PIP_INDEX_URL` 行为：当前 ARG 存在，但依赖安装硬编码清华源。
- [ ] 核实 `hermes-agent/Dockerfile` 的 `ghfast.top` 代理是否只用于当前服务器；生产环境需可配置或还原官方 URL。
- [ ] 核实 `hermes-agent/Dockerfile.bridge` 对 `web/node_modules` 和 `hermes_cli` 的 chown 是否覆盖所有 bridge build 写入点。
- [ ] **晚上重建 hermes bridge 镜像**（`nanobot-hermes-agent`，base 未改只建 bridge，带 `PIP_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple`）：Dockerfile.bridge 已预装 `edge-tts==7.2.7`（L68，LOCAL 注释）+ feishu extra。验证新用户首条消息不再卡 ~4 分钟——`/opt/data/logs/agent.log` 无 `Lazy-installing edge-tts`、收到消息后直接进 `conversation turn`。根因/查法见 CLAUDE.md「hermes 容器排查约定」。当前 accb21f7 不重建（已 lazy 装过，不受影响）。
- [x] **LLM provider 根因已修**（2026-07-02）：`config.py:57` `dedicated_hermes_default_provider` 默认 `"custom"`（hermes 不认→fallback auto→openrouter 401）。修复：`docker-compose.override.yml` gateway env 加 `PLATFORM_DEDICATED_HERMES_DEFAULT_PROVIDER=platform-gateway`，force-recreate gateway 已注入。**不用改代码/重建镜像**。核实项：新用户注册后看容器 `/opt/data/config.yaml` 的 `model.provider=platform-gateway` + `/opt/data/logs/agent.log` `provider=custom base_url=http://gateway:8080/llm/v1`。

## 测试覆盖

- [ ] 更新根 `tests/conftest.py` 和 `tests/README.md` 的默认 `OPENCLAW_BASE_URL`，避免默认打到失效的 `8080`。
- [ ] 增加部署脚本单元测试：端口推断、`--status`、`--clean`、`--rebuild`、Docker Desktop 自动启动分支。
- [ ] 增加 Compose 配置一致性测试：解析 `docker compose config`，断言脚本默认端口与 compose published port 一致。
- [ ] 补齐运行时端到端测试：注册/登录、创建 Hermes 用户容器、终端 `user=hermes`、技能 toggle、文件下载、飞书 onboarding。
- [ ] 复核 `platform/tests` 中 skip 的用例，区分“确实不适用”和“缺 mock/环境导致未覆盖”。
- [ ] 写操作测试继续遵守 `CLAUDE.md`：FakeDb 必须记录并断言 `commit`，不能只断言 `flush`。

## 运维与安全

- [ ] 处理 SSE / WebSocket 的 query token 泄漏：当前前端把 JWT 放在 `?token=`，Nginx access log 会记录完整 URL。
- [ ] 核实文件下载/预览 token query 的日志泄漏面，必要时改为短期一次性 ticket 或日志脱敏。
- [ ] 核实 `JWT_SECRET`、Postgres 默认密码、`PLATFORM_SHARED_HERMES_API_KEY` 等生产默认值是否在部署前强制覆盖。
- [ ] 为 gateway 日志中的 SQLAlchemy connection cleanup / CancelledError 建立复现和处理方案。

## 当前已验证事实

- [x] 当前 compose 服务运行中：gateway、frontend、manage-front、simple-front、postgres 均 Up，postgres healthy。
- [x] `http://127.0.0.1:9900/api/ping` 返回 `pong`。
- [x] `http://127.0.0.1:8080/api/ping` 当前连接失败，说明旧默认端口不可用。
- [x] `python3 -m py_compile` / `python3 -m compileall -q platform tests ...` 语法编译通过。
